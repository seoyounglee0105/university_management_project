<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.university.repository.interfaces.ProfessorRepository">
	<insert id="insertToProfessor">
		INSERT INTO professor_tb (name, birth_date, gender, address, tel, dept_id)
		VALUES (#{name}, #{birthDate}, #{gender}, #{address}, #{tel}, #{deptId})
	</insert>
	
	<select id="selectIdByCreateProfessorDto" resultType="integer">
		SELECT id FROM professor_tb 
		WHERE name = #{name}
		AND birth_date = #{birthDate}
		AND gender = #{gender}
		AND address =#{address}
		AND #{tel}
		AND #{deptId}
		
	</select>
	
	<insert id="insertToUser">
		INSERT INTO user_tb 
		VALUES (#{id}, #{password}, #{userRole})
	</insert>
</mapper>