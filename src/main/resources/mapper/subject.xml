<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.green.university.repository.interfaces.SubjectRepository">
	<insert id="insert">
		insert into subject_tb (name, professor_id, room_id, dept_id, type, sub_year, semester, time, grades, capacity, num_of_student)
		values(#{name}, #{professorId}, #{roomId}, #{deptId}, #{type}, #{subYear}, #{semester}, #{time}, #{grades}, #{capacity}, #{numOfStudent})
	</insert>

</mapper>